library IEEE;
use IEEE.STD_LOGIC_1164.ALL;

entity BARRAMENTO_2TO1 is
	port(
		A, B, Cin : in std_logic;
		S, Cout : out std_logic
	);
end entity;

architecture add of BARRAMENTO_2TO1 is
	begin
		S <= (Cin xor (A xor B));
		Cout <= (A and B) or ((A xor B) and Cin);
end architecture;